pragma solidity ^0.4.10;

import "token.sol";
import "bloomio_owned.sol";

contract BloomioToken is Token, BloomioOwned {
   //add naming for token
   function BloomioToken(address _bloomioKey, address _trading, uint256 _value) BloomioOwned(_bloomioKey, _trading) {
        balances[msg.sender] = _value;
        totalSupply = _value;
   }
 
   function transfer(address _to, uint256 _value) allowTransfer(_to) returns (bool success) {
        require(balances[msg.sender] >= _value);
        balances[msg.sender] -= _value;
        balances[_to] += _value;
        Transfer(msg.sender, _to, _value);
        return true;
    }

    function transferFrom(address _from, address _to, uint256 _value) returns (bool success) {
        require(balances[_from] >= _value && allowed[_from][msg.sender] >= _value);
        balances[_to] += _value;
        balances[_from] -= _value;
        allowed[_from][msg.sender] -= _value;
        Transfer(_from, _to, _value);
        return true;
    }

    function balanceOf(address _owner) constant returns (uint256 balance) {
        return balances[_owner];
    }

    function approve(address _spender, uint256 _value) allowTransfer(_spender) returns (bool success) {
        allowed[msg.sender][_spender] = _value;
        Approval(msg.sender, _spender, _value);
        return true;
    }

    function allowance(address _owner, address _spender) constant returns (uint256 remaining) {
        return allowed[_owner][_spender];
    }

    mapping (address => uint256) balances;
    mapping (address => mapping (address => uint256)) allowed;
}